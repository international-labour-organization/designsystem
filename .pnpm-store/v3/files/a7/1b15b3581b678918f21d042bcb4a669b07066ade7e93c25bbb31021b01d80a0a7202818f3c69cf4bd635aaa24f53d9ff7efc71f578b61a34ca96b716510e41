import { TaskGroup } from '../../types';
interface Fiber {
    child: Fiber | null;
    sibling: Fiber | null;
}
export declare function traverse(rootNode: Fiber, callback: (node: Fiber) => void): void;
declare const group: TaskGroup;
export default group;
