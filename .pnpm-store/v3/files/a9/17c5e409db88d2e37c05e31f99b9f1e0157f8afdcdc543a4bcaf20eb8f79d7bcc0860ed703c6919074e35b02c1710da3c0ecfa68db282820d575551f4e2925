/// <reference types="react" />
import { InteractionTask } from '../types';
declare type Args = {
    task: InteractionTask;
    getElement: () => React.ReactElement;
};
export default function runInteractionTask({ task, getElement }: Args): Promise<number>;
export {};
