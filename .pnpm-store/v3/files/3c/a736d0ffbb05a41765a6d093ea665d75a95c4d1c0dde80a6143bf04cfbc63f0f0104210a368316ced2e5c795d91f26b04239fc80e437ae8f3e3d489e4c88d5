/**
 * Fill map with value until map's size is size.
 *
 * @param {Map<*, *>} map
 * @param {number} size
 * @param {any} value
 */
export declare function fill(map: Map<any, any>, size: number, value: any): void;
