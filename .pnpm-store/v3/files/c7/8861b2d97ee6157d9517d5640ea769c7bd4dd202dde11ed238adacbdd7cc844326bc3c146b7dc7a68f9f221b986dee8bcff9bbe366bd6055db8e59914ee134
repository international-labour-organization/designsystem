import React from 'react';
import { TaskGroup, TaskGroupResult } from '../types';
declare type RunGroupArgs = {
    group: TaskGroup;
    getElement: () => React.ReactElement;
    samples: number;
};
export default function runGroup({ group, getElement, samples, }: RunGroupArgs): Promise<TaskGroupResult>;
export {};
