/// <reference types="react" />
import { Channel } from '@storybook/channels';
import { AllowedGroup, PublicInteractionTask } from '../types';
export default function Panel({ channel, interactions, allowedGroups, }: {
    channel: Channel;
    interactions: PublicInteractionTask[];
    allowedGroups: AllowedGroup[];
}): JSX.Element;
