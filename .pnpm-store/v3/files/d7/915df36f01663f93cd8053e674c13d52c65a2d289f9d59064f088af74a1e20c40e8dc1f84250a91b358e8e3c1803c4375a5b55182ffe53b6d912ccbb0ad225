import AppConfig from '../../AppConfig';
export declare function webpack(appConfig: AppConfig): {
    module: {
        rules: {
            test: RegExp;
            use: string[];
        }[];
    };
};
