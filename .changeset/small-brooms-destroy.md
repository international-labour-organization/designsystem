---
"@ilo-org/react": patch
"@ilo-org/styles": patch
"@ilo-org/twig": patch
---

Address accessibility issues with heading oreder in cards by changing `<h3>` tags to `<p>`, Additionaly add an optional prop that allows for configuration of the title element if needed.
