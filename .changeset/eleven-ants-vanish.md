---
"@ilo-org/react": minor
"@ilo-org/styles": minor
---

**Dropdown, SearchField, TextInput:** Add a new `size` property that defaults to `large`. Setting `size` to `small` will render new smaller versions. We'll be adding `small` versions to the rest of the form components soon. For the time being, this prop is only available to the React version of the form elements.
