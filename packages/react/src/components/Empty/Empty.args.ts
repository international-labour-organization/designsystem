import { EmptyProps } from "./Empty.props";

const empty1: EmptyProps = {
  className: "storybook",
};

/**
 * Sample prop definitions for Empty's enumerable properties (imported in stories and test)
 */
const EmptyArgs = {
  empty1,
};

export default EmptyArgs;
