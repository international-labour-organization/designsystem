import { Meta } from "@storybook/blocks";
import { LogoBuilder } from "./LogoBuilder";

<Meta title="Utilities/Logo Builder" />

# Logo Builder

The Logo Builder is an interactive tool that allows you to create and download ILO logos with custom subbrands.
You can choose from different logo variants, add custom subbrand text, and download the result as either PNG or SVG.

<LogoBuilder />
