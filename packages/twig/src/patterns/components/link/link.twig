{#
  LINK COMPONENT
#}
<a class="{{prefix}}--link {{prefix}}--link--{{theme}}" href="{{url}}"{% if target %} target={{target}} rel={target ? "noopener noreferrer": ''}{% endif %}>
  {% if label %}
    <span className="link__label">{{label}}</span>
  {% endif %}
</a>
