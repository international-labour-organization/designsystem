{#
  ACCORDION COMPONENT
#}
<ul class="ilo--accordion ilo--accordion--large" data-loadcomponent="Accordion">
    {% for item in items %}
      {% include "@components/accordion/accordion-item.twig" with {
        label: item.label,
        content: item.content,
      } only %}
    {% endfor %}
    </ul>
</div>
