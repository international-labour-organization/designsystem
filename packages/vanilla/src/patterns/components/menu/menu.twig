{#
/**
 * @file
 * menu
 *
 * Available variables for menu:
 *  - menu.variable_name [variable_type]: Description of the variable. Example below.
 *  - menu.invert [bool]: When set, colors will be inverted for light display on dark background.
 */
#}
<script>console.log({{ variant }})</script>
{% if variant == "primary" %}
  {% include '@components/menu/_menu--primary.twig' with {variant: 'primary', items: items} %}
{% elseif variant == "footer" %}
  {% include '@components/menu/_menu--footer.twig' with {variant: 'footer', items: items} %}
{% endif %}
